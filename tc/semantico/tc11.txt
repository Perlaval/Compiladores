class Mundo{
    Bool c;
    Int a;
    pub Str b;
    Prueba d;
}

impl Mundo{
    .(Str saludo, Int num){ a = 42;}
    fn Int get_a(){ ret a; }
    st fn imprimo_algo(){
        (IO.out_str("hola mundo"));
    }
}

class Prueba{
    Mundo c;
    Str y;
    Int z;
    B claseB;
}

impl Prueba{
    .(){

        Prueba h;
        c = new Mundo(4, "hola");
        y = c.b; // Acceso correcto al atributo de la clase
        z = c.a; // Acceso incorrecto al atributo de la clase
        z = c.get_a(); // Acceso correcto al atributo de la clase
        (c.imprimo_algo());
        //c.a = c.get_a();
        c.b = self.y;
        h = new Prueba();

    }
    fn pruebaB(){
        //self.claseB.llamarPrueba(); //CORREGIR: Se esperaba 'opIgual' y se encontro 'parAbre'
    }
}

class B : Prueba{
    Prueba l; //CORREGIR: me esta error pero habia colocado l Prueba: Se esperaba 'llaveCierra' y se encontro 'idMetVar'
}

impl B{
    .(){
        Str j;
         j = l.y; //No deberia dejar pq y es un atributo privado de l (clase Prueba)
    }
    fn llamarPrueba(){
        self.l.pruebaB();
    }
}

start{
    //h = new Prueba(); //CORREGIR: Id: 'h' no declarado en la clase: Prueba
    //Prueba h; // CORREGIR: Se esperaba 'llaveCierra' y se encontro 'idClass' |
    //h.z = 3;
    //Int a_Mundo; //Se esperaba 'llaveCierra' y se encontro 'tInt': es porque la declaracion no esta en el lugar correcto
    //a_Mundo = h.c.a;
    //(} //1. Mejorar error: Se esperaba un operando y se encontro llaveCierra
    //(IO.out_str("hola mundo"));
    Int a; //what: Se esperaba 'llaveCierra' y se encontro 'tInt' -> este error es porque la disposicion de la declaracion es incorrecta: o sea las declaraciones van antes de las sentencias ((IO.out_str("hola mundo")); a_Mundo = h.c.a;) SIEMPRE.
    Str c;
    Str b;
    /*Fibonacci fib; //2. MEJORAR: La clase: 'Fibonacci' nunca fue definida
    Int n;
    fib = new Fibonacci();
    n = IO.in_int();
    (IO.out_int(fib.sucesion_fib(n)));*/
    b="3.0";
    a=10;
    c = "hola";
    //c = b + c; ERROR: estoy intentando concatenar dos string
    //c=a+a; //esto devuelve error de tipo
    (IO.out_str(c));
    //IO.out_str(c); es un error pq para hacer una llamadaMetodo como esta. Tiene que ser por medio de SentenciaSimple -> ( Expresion )

}